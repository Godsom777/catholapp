import 'package:flutter/material.dart';

class YearReadingsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Year Readings'),
      ),
      body: Center(
        child: Text('Hello, World!'),
      ),
    );
  }
}